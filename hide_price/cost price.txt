<!-- Adding cost price to kanban view and make it by deafult visabile -->
    <!-- <record id="product_kanban_invisible_cost_price" model="ir.ui.view">
    <field name="name">product_kanban_invisible_cost_price</field>
    <field name="model">product.template</field>
    <field name="inherit_id" ref="product.product_template_kanban_view"/>
    <field name="arch" type="xml">
        <!-- <div name="product_lst_price"> -->
        <div name="product_lst_price">
        <!-- Content of product_lst_price -->
        </div>
            <div name="hide_cost">
                Cost: <field name="standard_price"/>
            </div>
        <!-- </div> -->
        <xpath expr="//div[@name='hide_cost']" position="attributes">
            <attribute name="invisible">0</attribute>
        </xpath>
    </field>
</record> -->

----------------------------------------------
python

  @api.onchange('cost_price')
    def _onchange_cost_price(self):
        if self.cost_price:
            self.env['ir.ui.view'].search([('name', '=', 'product_kanban_invisible_cost_price')]).write({'arch': '<xpath expr="//div[@name=\'hide_cost\']" position="attributes"><attribute name="invisible">0</attribute></xpath>'})
        else:
            self.env['ir.ui.view'].search([('name', '=', 'product_kanban_invisible_cost_price')]).write({'arch': '<xpath expr="//div[@name=\'hide_cost\']" position="attributes"><attribute name="invisible">1</attribute></xpath>'})

